v2.2.0
    changed dispatch function to dispatchCustom function and changed dispatch function to only require a name (+ optional target)
    updated package to latest config templates

v2.3.0
    added UCFirst function to library, which makes the first letter of a string uppercase